# minix

Un Window Manager minimalista de mosaico (tiling) para X11 escrito en C++.

## 🚀 Requisitos

- C++ compiler (`g++`)
- X11 development libraries (`libx11-dev` en Debian/Ubuntu o `libx11` en Arch)

## 🛠️ Compilación

```bash
g++ main.cpp -o minix -lX11
